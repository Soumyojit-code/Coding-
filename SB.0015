// check a number is buzz number or not 
#include<stdio.h>
int main(){
    int n;
    printf("Enter a number: ");
    scanf("%d",&n);
    if(n%7==0&& n%10==7)

        printf("%d buzz number",n);
    else
        printf("%d not buzz number",n);

    return 0;
}
